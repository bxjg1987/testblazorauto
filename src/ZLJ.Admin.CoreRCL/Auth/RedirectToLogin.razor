@inject NavigationManager NavigationManager

@code {

    [Parameter] public string? ReturnUrl { get; set; }

    protected override void OnInitialized()
    {
        var url = ReturnUrl ?? Uri.EscapeDataString(NavigationManager.Uri);
        NavigationManager.NavigateTo($"Account/Login?returnUrl={url}", forceLoad: true);
    }
}
