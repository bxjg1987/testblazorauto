@using ZLJ.App.Admin.Post;
@using ZLJ.App.Admin.Post.Dto;
@* @attribute [Authorize(PermissionNames.AdministratorBaseInfoPostCreate)] *@

@inherits AdminCreateBaseComponent<IPostAppService, PostDto, int, PagedAndSortedResultRequest< PagedPostResultRequestDto>, CreatePostDto, PostEditDto>
@* <ValidateForm Model="createDto" @ref="validateForm">
    <EditorForm Model="@createDto" RowType="RowType.Inline" LabelAlign="Alignment.Right">
        <FieldItems>
            <EditorItem @bind-Field="@context.OuIds" Context="ctx">
                <EditTemplate>
                    <SelectTree TValue="long" Items="items">

                    </SelectTree>
                </EditTemplate>
            </EditorItem>
            <EditorItem @bind-Field="@context.GrantedPermissions" Editable="false" />
        </FieldItems>
    </EditorForm>
</ValidateForm> *@