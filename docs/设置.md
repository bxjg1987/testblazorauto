# 设置管理
需要提供界面让用户来修改设置。

## 应用级别
这个前期可以直接在数据中去修改，或者
仅允许户主管理，租户都不需要查看
后台管理应用提供此功能

## 租户级别
有权限的就可以修改。
后台管理应用提供此功能
优先实现此功能。因为应用级别的可以手动在数据库设置，用户级别的只是增强功能

## 用户级别
这个应该是定义在common应用中，因为不同类型的应用可能用户自己需要做自己的设置。
能设置的项目是 只要设置当以中包含了用户scope的，如果abp够聪敏的话，在更新的值与应用或租户重叠时，就不应该单独存储值


# 当前的实现
这里仅说明api端的内容，前端在单独的文档中说明，不过两个端设计方式差不多。

abp有自己的setting，分应用、租户和用户级别。
.net core 有自己的配置系统。

某些非abp模块会直接使用.net core的配置系统（包括options模式），
希望这部分还是使用.net 自己的配置模式。仅获取配置。
注意，它仅关注应用级别的配置，目前不考虑租户和用户。

让更新使用abp的setting模块，

主体思路是自定义配置提供程序，参考：BXJG.Utils.EFCore.Settings
AbpSettingsConfigurationProvider经过测试是单例的

settingmanager修改配置时，通过全局事件zhongjie通知，AbpSettingsConfigurationProvider，通过OnReload通知IConfiguration，源已更新

由于abp的接口项目中program中的 host构建方式，只能在program中启动配置，

还需要注意，settingmanager批量修改配置时，可能几秒内，修改多个配置项，没必要每次刷新，因为要读数据库
所以使用YanchiChuli延迟覆盖执行。

AbpSettingsConfigurationProvider、YanchiChuli 这种组件没有用ioc，但需要提供默认日志
所以使用SimpleLogger作为默认日志记录器。


发布包到nuget的脚本


# 集成.net自己的配置和abp setting的应用程序级别的配置


.net core的配置有个功能是配置源更新后，获取配置可以拿到最新值。


实现思路
自定义配置源和提供器
配置提供器在启动时，直接从数据库查询，因为启动时还没有服务注册，没法用ISettingManager或存储库
并注册全局事件总线。变化时通知外部。

后续abp事件监控设置实体修改事件，触发事件，可以用Zhongjie.Instance

或者参考这个：https://makolyte.com/event-driven-dotnet-how-to-use-query-notifications-in-sql-server-to-monitor-database-changes/
监控sqlserver 得到变更通知。上面的方式不需要数据库特殊配置，也不需要关心具体数据库


注意：IConfiguration内部引用ConfigurationProvider的，所以提供器的值更新，配置应该也可以拿到更新，只是没通知应用程序。

.net core 自定义提供器的通知机制

# 另一种更简单的方式
按约定，在开发中，应用配置通通使用.net core自己的方式，不要使用的abp的方式。
只有租户或用户相关配置才使用abp的方式，这样就不需要做任何更改和适配

# 前后端配置同步
前端server时，通过scope隔离配置
wasm时，单例。

前端更是需要.net原生配置方式。若按上面的简单配置约定，应用级别的配置压根没使用abp的方式，那么前端应用级别的配置可以放心使用.net的方式。
但是如何同步配置呢？
一种方式是把api和前端需要的配置放独立的json文件中，双发都add进来。
或者干脆两边独立复制一份。
后端和前端部署在一起时有用。

另一种方式是，前端调用api从后端获取。前端启动时获取并配置。由于仅关注应用级别的配置，这也不是不行。
这样前端仅保留后端api地址的配置节点
前后端独立部署时有用，但需要处理配置更新的问题。可以考虑通过定时任务或signalR主动推送

## 租户和用户级别的配置同步
这个是getall中，已经包含了，这种使用abp的方式前端读取就行，不要使用.net core默认的配置api，因为这是租户和用户相关的。

# 配置服务
用三方库，应用级别的配置用统一配置中心，用户和租户的用abp自己的api